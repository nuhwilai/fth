import { IncludeArrayPipe } from './include-array.pipe';

describe('IncludeArrayPipe', () => {
  it('create an instance', () => {
    const pipe = new IncludeArrayPipe();
    expect(pipe).toBeTruthy();
  });
});
